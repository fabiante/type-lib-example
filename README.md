# Type Lib Example

## Usage

This project requires:

- yalc
- Taskfile

---

Install dependencies:

```shell
task install
```

Start compilation of all projects in watch mode:

```shell
task dev -w
```
